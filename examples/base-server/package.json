{
  "name": "@iamtoxa/md-example-base-server",
  "version": "0.0.0",
  "private": true,
  "description": "Приложение сервера md-engine (Bun).",
  "license": "Apache-2.0",
  "author": "iamtoxabi",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist || rm -rf dist",
    "start": "bun ./dist/index.js"
  },
  "dependencies": {
    "@iamtoxa/md-engine-server": "workspace:*"
  }
}
